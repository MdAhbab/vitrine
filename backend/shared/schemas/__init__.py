"""Pydantic request/response schemas — the wire contract with the frontend.

These intentionally mirror the TypeScript types in
`frontend/src/app/lib/mockData.ts` and `store.ts` so responses drop straight
into the existing UI. See backend.md "Frontend <-> Backend wiring".
"""
